import { BrainElementDirective } from './brain-element.directive';

describe('BrainElementDirective', () => {
  it('should create an instance', () => {
    const directive = new BrainElementDirective();
    expect(directive).toBeTruthy();
  });
});
